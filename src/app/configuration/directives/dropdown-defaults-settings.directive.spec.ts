import { DropdownDefaultsSettingsDirective } from './dropdown-defaults-settings.directive';

describe('DropdownDefaultsSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDefaultsSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
